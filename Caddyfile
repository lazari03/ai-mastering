# Terminates HTTPS and routes to the two containers that need to be
# public — frontend and the Node API (Python's port 8001 stays internal to
# the Docker network, it's never reached from outside; see ARCHITECTURE.md
# §7 on why it has no auth of its own).
#
# Caddy gets a cert from Let's Encrypt automatically the first time it
# starts, using this domain — no manual certbot/nginx config. Requires
# auralithforge.app's DNS A/AAAA record already pointing at this server's
# IP before you start Caddy, or the ACME challenge fails.

(security_headers) {
	# Brotli/gzip for text responses — was off entirely, meaning every
	# HTML/CSS/JS response went out uncompressed. This alone is usually a
	# large chunk of a bad PageSpeed score (bigger transfer = slower on a
	# throttled connection, which is exactly what Lighthouse simulates).
	encode zstd gzip
	header {
		# Force HTTPS on repeat visits — 1 year, apex + subdomains.
		Strict-Transport-Security "max-age=31536000; includeSubDomains"
		# Stops a browser from guessing content-type in a way that turns
		# an upload into executable script.
		X-Content-Type-Options "nosniff"
		# No embedding this site in an <iframe> elsewhere (clickjacking).
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
		Permissions-Policy "camera=(), microphone=(), geolocation=()"
		# Not a full CSP — this app's Next.js bundle needs inline
		# hydration scripts, and a naive default-src would break it. A
		# real CSP needs deliberate per-directive tuning, not a
		# copy-pasted one; left as a follow-up rather than guessed at.
		-Server
	}
}

auralithforge.app {
	import security_headers
	# Next.js sets Cache-Control: s-maxage=31536000, stale-while-revalidate
	# on page HTML/RSC responses by default — tuned for a CDN edge (Vercel)
	# that can purge/on-demand-revalidate the moment a new deploy lands.
	# Self-hosted behind a plain reverse proxy with no such CDN, that
	# header instead tells the browser's own cache (or anything else in
	# the chain) it's safe to hold onto the old page for up to a year —
	# exactly the "I deployed, but visitors still see the old site" bug.
	# Everything under /_next/static/ is deliberately untouched — those
	# filenames are content-hashed, genuinely safe (and desirable) to
	# cache for a year, a new deploy just gets new filenames.
	@html {
		not path /_next/static/*
	}
	# Two prior attempts at overriding Next.js's own Cache-Control via the
	# top-level `header` directive both failed empirically — it runs
	# before reverse_proxy receives the upstream response, so it was
	# fighting the ordering rather than actually replacing anything (a
	# plain set left both values on the wire; a delete+set block deleted
	# both). header_down, scoped inside reverse_proxy itself, is the
	# directive actually meant for "modify a header on the response this
	# proxy just received from its upstream" — it runs at the right point
	# in the pipeline for this, by construction, not by ordering luck.
	reverse_proxy frontend:3000 {
		header_down @html -Cache-Control
		header_down @html Cache-Control "no-cache, must-revalidate"
	}
}

api.auralithforge.app {
	import security_headers
	reverse_proxy node-api:8000
}
