// Homepage "hear the difference" section — real before/after clips. Files
// live in /public/audio/demos/ (not committed here, since audio isn't
// something to fabricate — see BeforeAfterPlayer.jsx). Add more entries
// once more real mastered pairs exist.
export const BEFORE_AFTER_DEMOS = [
  { genre: "Pop", label: "Pop — Vocal-Forward Mix", beforeSrc: "/audio/demos/pop-before.mp3", afterSrc: "/audio/demos/pop-after.mp3" },
];
